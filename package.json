{
  "name": "enzlab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:all",
    "build:simulator": "tsc -b && vite build && cp dist/index.html EnzLab_Simulator.html",
    "build:decoder": "tsc -b && vite build -c vite.decoder.config.ts && cp dist/decoder.html EnzLab_Decoder.html",
    "build:all": "npm run build:simulator && npm run build:decoder",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "ml-levenberg-marquardt": "^4.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
